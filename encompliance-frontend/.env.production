# API Base URL - In production, this should be a relative URL
VITE_API_BASE_URL=/api/v1

# Backend URL - In production, use relative URL 
# This works because nginx will proxy /api requests to the backend
VITE_BACKEND_URL=/ 